import React, {FC} from 'react';
import {Layout} from 'antd';

const {Footer} = Layout;

const FooterComponent: FC = () => {
  return <Footer style={{textAlign: 'center'}}>@kanade0404 ©2021</Footer>;
};

export default FooterComponent;
